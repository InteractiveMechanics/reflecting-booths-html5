import keyMirror from 'keymirror';

export const states = keyMirror({
  WELCOME: true,
  UI_SETTINGS: true,
  ABOUT: true,
  LANGUAGE_SELECT: true,
  QUESTIONS: true,
  RECORD: true,
  SUBMIT: true,
  CONFIRM: true
});
